export interface Props {
    dark?: boolean;
    setDark?: any;
    checked?: boolean;
    setChecked?: any;
    completed?: boolean;
    value?: string;
}

export interface Todo {
    id: number;
    taskName: any;
    completed: boolean;
}

